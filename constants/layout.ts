// Shared base-page layout metrics. Use via components/ScreenLayout so every
// tab screen has consistent padding/margins (matching the home page).

export const LAYOUT = {
  screenPaddingH: 20,
  screenPaddingTop: 16,
  screenPaddingBottom: 40,
  titleMarginBottom: 16,
  sectionGap: 20,
};

// Modern, consistent corner radii. Cards are generously rounded; controls a bit
// tighter; pills fully round.
export const RADIUS = {
  sm: 10,
  control: 12,
  card: 16,
  lg: 20,
  pill: 999,
};
